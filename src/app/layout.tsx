import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Solar Energy Calculator | Estimate Your Solar Potential",
  description: "Calculate your solar energy potential with precision. Get detailed estimates for energy production, system sizing, CO₂ savings, and financial benefits based on your location and specifications.",
  keywords: "solar energy, solar calculator, renewable energy, solar panels, energy estimation, CO2 savings",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
        suppressHydrationWarning={true}
      >
        {children}
      </body>
    </html>
  );
}
